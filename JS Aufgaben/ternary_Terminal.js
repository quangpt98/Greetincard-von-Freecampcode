let number = -10;

let message = number >= 0 ? "Số dương": "Số âm"

console.log (message)

let note =5;

let check = note >= 8 ? "Giỏi":(note>=6.5? "Khá":(note>=5 ? "Trung bình":"Yếu "));
console.log(check)